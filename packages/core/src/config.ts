// Единственное место для всех чисел баланса InkArena (Р44).
// Логика в sim/spellcraft/recognition/geometry обращается к этим значениям —
// магических констант в коде быть не должно.
export const config = {
  geometry: {
    // Насколько близко (по зазору между bounding box, в долях большей
    // диагонали пары) два штриха должны лежать, чтобы попасть в один кластер.
    clusterProximityRatio: 0.6,
  },
  recognition: {
    // Число точек, до которого ресемплируется штрих перед сопоставлением $P.
    // Компромисс точность/скорость (алгоритм $P, Vatavu et al. 2012).
    resamplePoints: 32,
    // Нормировочная дистанция: средневзвешенное расстояние между облаками
    // точек (в нормированных координатах ~[-0.5, 0.5]), при котором точность
    // падает до нуля. Меньше — строже оценка руки.
    scoreRefDistance: 0.4,
    // Порог распознавания: ниже этой точности глиф считается нераспознанным
    // (наверх уходит как кандидат в осечку). Р36 — калибруется плейтестом.
    minAccuracy: 0.7,
    // Детекция замыкающего круга (не через $P, Р27): замкнутость + охват.
    circle: {
      // Минимум точек штриха, ниже которого форму даже не оцениваем (мусор/клик).
      minPoints: 8,
      // Число точек, до которого ресемплируется штрих перед оценкой круглости.
      resamplePoints: 64,
      // Зазор между началом и концом штриха в долях диагонали bbox.
      maxEndpointGapRatio: 0.25,
      // Разброс радиусов от центроида (σ/среднее): у круга ~0.01, у шумного
      // круга ~0.07, у квадрата ~0.11. Порог отделяет круг от квадрата.
      maxRadiusCoV: 0.09,
      // Суммарный модуль поворота (рад): у замкнутой петли ≈ 2π.
      minTurning: 5.0,
    },
  },
  spellcraft: {
    // Роль глагола в композиции (Р25). primary заменяет форму действия
    // (щит/хил вместо атаки); augment — модификатор поверх атаки (баф/дебаф
    // усиливают/ослабляют, но каст остаётся атакой). Разбиение — интерпретация
    // Р25 под корпус Р32 (Проклятие камня = атака-снаряд с дебафом, Гнев бури =
    // атака-нюк с бафами); окончательная форма действия — за sim (#38),
    // открытый вопрос.
    verbRoles: {
      shield: 'primary',
      heal: 'primary',
      buff: 'augment',
      debuff: 'augment',
    },
    // Классификация глифа относительно замыкающего круга (Р26 п.6, Р27).
    // Кольцо неоднозначности шириной boundaryMarginRatio·R вокруг радиуса R:
    // глиф целиком внутри внутренней границы — «внутри»; пересекает кольцо —
    // «на границе» (осечка); целиком за внешней границей — «снаружи» (круг не
    // охватил, «не отправлено»). Р36 — калибруется плейтестом.
    boundaryMarginRatio: 0.12,
    // Допуск положения ядра от центра круга в долях радиуса (Р26 п.2 «в
    // центре»). Ядро дальше этого от центра — композиция не читается как
    // «ядро и орбиты». Открытый параметр баланса (Р36).
    coreCenterToleranceRatio: 0.6,
    cost: {
      // Чернила за каждый нарисованный глиф-штрих (база за знак, Р21).
      perGlyph: 4,
      // Чернила за единицу длины линии композиции (плата за росчерк, Р21).
      // Замыкающий круг в стоимость не входит (Р27).
      perInkLength: 0.01,
    },
    power: {
      // База магнитуды по стихии ядра (Р4: «что нарисовано»). Пентаграмма
      // контрпика — в sim (#38), здесь только собственная сила стихии.
      elementBase: {
        fire: 10,
        water: 10,
        air: 10,
        earth: 10,
        lightning: 10,
      },
      // Вклад глагола в базу, домножается на относительный вес глифа (Р26 п.5).
      verbBase: {
        shield: 8,
        heal: 8,
        buff: 5,
        debuff: 5,
      },
      // f(размер) — трейд-офф размера (Р4, Р30): монотонная степенная кривая
      // от диагонали ядра к refDiagonal, зажатая в [min, max]. Конкретная
      // форма (exponent/границы) — открытый параметр баланса (Р36,
      // game/spellcraft.md#открытые-параметры).
      size: {
        refDiagonal: 280,
        exponent: 0.5,
        min: 0.5,
        max: 1.6,
      },
    },
  },
} as const;

export type Config = typeof config;
