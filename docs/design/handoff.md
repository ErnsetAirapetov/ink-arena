# Handoff — состояние работы

**Обновлено:** 2026-07-18 (пауза на обновление харнеса) · **Ветка:** `main`

## СТОП-режим

Владелец обновляет оснастку (.claude/) и велел: после возврата агента #38
НИЧЕГО не стартовать — ни ревьюеров, ни исполнителей — до его разрешения.
PR от #38 (если придёт) висит без ревью и мержа. Продолжение — по команде.

## Где мы

**Веха 0** — закрыта документально; остался ОДИН пункт: бумажная партия
владельца по `paper-playtest.md` (кит готов), итоги → комментарий в #13 и
записи в `../decisions.md`. Эксперимент-спайк отменён (Р42).

**Веха 1** — конвейер `tech/architecture.md`, трекинг #40:
- [x] #35 monorepo: снос прототипа (тег prototype-archive), packages/core+client
- [x] #36 geometry + recognition ($P, словарь v0, golden Р32) — PR #44
- [x] #37 spellcraft-парсер (CastOutcome: spell/misfire/not-sent) — PR #46
- [x] #45 follow-up распознавания (жёсткие искажения, KNOWN_FRAGILE) — PR #48
- [~] #38 core/sim — ГОТОВ, PR #51 ЖДЁТ РЕВЬЮ (стоп-режим: ревьюер не
      запускался). Ветка feat/core-sim в worktree агента. При возобновлении:
      1) ревьюер на PR #51 (opus; ветка форкнута до #48 — сперва подтянуть
      main, конфликт config.ts маловероятен: sim-секция vs recognition);
      2) обратить внимание на решения агента: step(state, inputs) без
      третьего аргумента, пентаграмма разрешается на щите (голое попадание
      нейтрально), снаряды без позиции (полёт = таймер), корпус бота с
      номинальной стоимостью чернил; 3) после мержа — запуск #39.
- [ ] #39 client: первый играбельный билд — точка проверки риска Р22 (Р42)

`npm test` на main: 229 зелёных. Бэклог не в конвейере: #47 (сегментация
многоштриховых глифов, после #39), #49 (ротационная разделимость форм
earth/water/heal — желательно до плейтеста #39, не блокер).

## Очередь needs-decision владельцу (не блокирует конвейер)

#23 главное меню · #24 экран результатов · #25 допуск в турниры ·
#26 контент задачи дня/прописей · #28 артефакты (добыча/респек/слоты).
Можно закрыть proposals-разбором, как в фазе 0.

## Процессные заметки для следующей сессии

- Оркестратор — Индиго (Р40): декомпозиция, доска, ревью, мерж; подпись
  «— Индиго» в issues/PR. Модели: исполнители-код и ревьюеры — opus,
  рутина/доки — sonnet (поле «Модель» в issue главнее).
- Типы агентов из .claude/agents/ в старой сессии не резолвились —
  эмулировались через general-purpose + «прочитай свою роль» + model.
  После рестарта проверить `subagent_type: "impl"` — должно заработать.
- Владельцу предлагались патчи settings.json (сам файл прав не редактирую):
  matcher хука guard «Bash|PowerShell» (критично) и allow для
  git commit/push/stash/merge/mv. Статус — на владельце; возможно, войдёт
  в его обновление харнеса.
- Уборка за агентом: worktree называются `.claude/worktrees/agent-<id>`,
  каталог искать по ветке (`git worktree list --porcelain`); перед rm
  убедиться, что шелл не стоит внутри и не висят vite/esbuild-процессы.
- PR мержим последовательно; параллельные PR не должны трогать общие файлы
  (README/roadmap обновляет оркестратор батчем — урок конфликта #16/#19).

## Договорённости с владельцем

- Коммиты: Conventional Commits по-русски, автор только
  `Air-Erik <eriktarakan@gmail.com>`, никаких упоминаний ассистентов.
- Изменение закреплённого решения — новая запись «пересмотрено» в
  `../decisions.md`; решение без записи не существует.
- `game/` — источник правды; баланс — только `packages/core/src/config.ts`.
- Кодовые задачи — только через доску (issue → worktree → PR → ревью →
  squash-merge → уборка).
