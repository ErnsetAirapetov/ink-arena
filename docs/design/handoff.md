# Handoff — состояние работы над фазой 0

**Обновлено:** 2026-07-05 · **Ветка:** `docs/game-design` (PR в `main` открыт)

## Контекст в двух абзацах

InkArena перезапускается: MVP-прототип (в `src/`, работает, 73 теста зелёные)
доказал ядро — рисование глифов, распознавание $P, точность/размер → сила,
стихии, щиты. Решено переписать игру почти с нуля, но сначала пройти
пропущенную фазу 0: полностью описать, что за игра. Целевая архитектура кода
(обсуждена, не записана в доки): детерминированная симуляция ядром, monorepo
`core/client/server` на TypeScript, web-first + PWA/Capacitor, mobile-first.

Процесс фазы 0 — конвейер (см. `docs/README.md`): вопросы (`questions.md`) →
сырые ответы владельца (`answers.md`) → закреплённые решения (`decisions.md`,
Р1–Р20) + открытые темы (`discussion.md`, Т1–Т9) → потом GDD в `docs/game/`.

## Где мы сейчас

1. Ответы на рамку A–H и часть волны 2 — получены и разобраны.
2. Закреплено 20 решений (Р1–Р20) — `decisions.md`.
3. Три главные развилки разобраны с вариантами и рекомендациями —
   `discussion.md`: **Т1** грамматика (реком.: «ядро и орбиты», цепочки позже),
   **Т2** темп (реком.: параллельный реалтайм + раунды 60–90 с),
   **Т3** прогрессия (реком.: горизонтальная + стандартные спорт-форматы).
4. **Последний шаг:** написан `proposals.md` — черновые решения по ВСЕМ
   оставшимся вопросам (три развилки + C1.1, C4.1–2, D1.1, D2.1, D3.1, D5.1,
   D6.1, C7.1, B2.1, G1.1, E1.1, E2.1, E5, H5, F3) в формате
   решение/почему/альтернатива.

## Следующие шаги (в порядке)

1. **Владелец** читает `proposals.md`, помечает `✅` / `✏️ + правки` прямо
   в файле, дозаполняет `answers.md` (важнее всего: D1.1 — зарисовки
   заклинаний, если видение отличается от предложенного корпуса).
2. **Клод** по меткам: согласованное → `decisions.md` (новые Р-записи),
   ответы → `answers.md`, закрытые темы вычеркнуть из `discussion.md`.
3. Когда Т1–Т3 закреплены — **кристаллизация `docs/game/vision.md`**
   (первый файл GDD), затем `game/spellcraft.md`.
4. Параллельно/потом — два дешёвых эксперимента из H5:
   спайк давления на текущем прототипе + бумажная проверка грамматики v0.

## Договорённости с владельцем (важно для ассистента)

- Коммиты: Conventional Commits по-русски, автор только
  `Air-Erik <eriktarakan@gmail.com>`, никаких упоминаний ассистентов.
- `answers.md`/`braindump.md` пишет только владелец, не править задним числом.
- Изменение закреплённого решения — новая запись «пересмотрено», не правка.
- Сырые файлы коммитим отдельно от разбора (история: ответы → анализ).
