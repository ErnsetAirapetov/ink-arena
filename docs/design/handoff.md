# Handoff — состояние работы над фазой 0

**Обновлено:** 2026-07-18 · **Ветка:** `docs/game-design` (PR в `main` открыт)

## Контекст в двух абзацах

InkArena перезапускается: MVP-прототип (в `src/`, работает, 73 теста зелёные)
доказал ядро — рисование глифов, распознавание $P, точность/размер → сила,
стихии, щиты. Решено переписать игру почти с нуля, но сначала пройти
пропущенную фазу 0: полностью описать, что за игра. Целевая архитектура кода
(обсуждена, не записана в доки): детерминированная симуляция ядром, monorepo
`core/client/server` на TypeScript, web-first + PWA/Capacitor, mobile-first.

Процесс фазы 0 — конвейер (см. `docs/README.md`): вопросы (`questions.md`) →
сырые ответы владельца (`answers.md`) → закреплённые решения (`decisions.md`)
→ кристаллизация GDD в `docs/game/`.

## Где мы сейчас

1. **Фаза вопросов ЗАВЕРШЕНА.** 2026-07-18 владелец разобрал `proposals.md`
   в интерактивной сессии и принял все предложения без правок.
2. Закреплено **37 решений** (Р1–Р37) — `decisions.md`. Ключевые новые:
   Р21 грамматика «ядро и орбиты», Р22 реалтайм + раунды 60–90 с,
   Р23 горизонтальная прогрессия, Р26 шесть правил компоновки,
   Р29 осечка без стана, Р32 корпус из 5 эталонов, Р36 два эксперимента.
3. `discussion.md` — **все темы Т1–Т9 закрыты**, файл — история.
4. `questions.md` — открытых вопросов нет; зарисовки D1.1 — опциональная
   домашка владельца в `braindump.md`.

## Следующие шаги (в порядке)

1. **Кристаллизация `docs/game/vision.md`** — первый файл GDD: концепт на
   страницу (фантазия, игрок, уникальность, анти-скоуп) из Р1–Р37.
2. **`docs/game/spellcraft.md`** — ядро: грамматика (Р21, Р24–Р27, Р32),
   чернила, осечки (Р29), формула силы (Р4, Р30).
3. Остальные файлы `game/` по мере готовности: combat (Р22, Р28, Р31),
   progression (Р23, Р34), ux (Р35), world (Р19, Р37), modes.
4. **Два эксперимента Р36** (параллельно, до большого переписывания):
   спайк давления на текущем прототипе + бумажная проверка грамматики v0
   на корпусе Р32. Результаты могут откалибровать тайминги Р22 и штраф Р29.
5. После ядра `game/` — записать целевую архитектуру в `docs/tech/`.

## Договорённости с владельцем (важно для ассистента)

- Коммиты: Conventional Commits по-русски, автор только
  `Air-Erik <eriktarakan@gmail.com>`, никаких упоминаний ассистентов.
- `answers.md`/`braindump.md` пишет только владелец, не править задним числом.
- Изменение закреплённого решения — новая запись «пересмотрено», не правка.
- Сырые файлы коммитим отдельно от разбора (история: ответы → анализ).
- `game/` — источник правды; при противоречии с `design/` прав `game/`.
