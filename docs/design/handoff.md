# Handoff — состояние работы

**Обновлено:** 2026-07-18 (вторая сессия) · **Ветка:** `docs/game-design`
(PR в `main` открыт)

## Контекст

Фаза 0 (полное описание игры до переписывания кода). Фаза вопросов
ЗАВЕРШЕНА: всё закреплено в `../decisions.md` (Р1–Р39), темы Т1–Т9 закрыты.
Идёт кристаллизация GDD в `../game/` и работа по новому процессу:
оркестратор + субагенты, доска — GitHub Issues (`gh` установлен и
авторизован), кодовые задачи — только через issue + worktree + PR.

## Где мы сейчас

1. Р1–Р39 закреплены; `../game/vision.md` готов и закоммичен.
2. Корень `docs/` реорганизован (issue #3, Р39): README, goals,
   architecture, decisions, risks, roadmap. Журнал решений переехал из
   `design/` в корень — единый на весь проект.
3. `../game/spellcraft.md` — ГОТОВ: issue #2 закрыт, PR #4 смержен в
   `docs/game-design`, worktree и ветка удалены. Полный цикл
   issue → worktree → PR → merge пройден впервые.

## Следующие шаги

1. `../game/combat.md` — следующий файл GDD (Р16, Р22, Р28, Р31).
2. Эксперименты Р36: спайк давления (кодовая задача — через issue) и
   бумажная проверка грамматики на корпусе Р32.
3. Остальные `game/`: modes, progression, ux, world — по мере готовности.
4. После ядра `game/` — детальные спеки `tech/`.

## Договорённости с владельцем (важно для ассистента)

- Коммиты: Conventional Commits по-русски, автор только
  `Air-Erik <eriktarakan@gmail.com>`, никаких упоминаний ассистентов.
- `answers.md`/`braindump.md` пишет только владелец, не править задним числом.
- Изменение закреплённого решения — новая запись «пересмотрено», не правка.
- `game/` — источник правды; при противоречии с `design/` прав `game/`.
- Числа баланса — только в `src/config.ts`, в доках не фиксируются.
